using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyTraprod : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }
    public void destroytrap()
    {
        Destroy(gameObject);
    }
    // Update is called once per frame
    void Update()
    {
        
    }
}
